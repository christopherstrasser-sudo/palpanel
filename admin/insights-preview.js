(() => {
  const root=document.getElementById('insightsPreview');
  if(!root)return;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  let loading=false,loaded=false;
  async function load(){if(loading||loaded||document.getElementById('adminView')?.classList.contains('hidden'))return;loading=true;try{const r=await fetch('/api/admin/insights',{credentials:'same-origin',headers:{Accept:'application/json'}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);const score=Math.round(d.health?.score||0),rows=(d.insights||[]).slice(0,3);root.innerHTML=`<div class="insights-preview-head"><div><span class="eyebrow">COMMUNITY INTELLIGENCE</span><h2>Automatische Community-Auswertung</h2></div><a class="btn" href="/admin/insights">Alle Insights öffnen</a></div><div class="insights-preview-grid"><div class="preview-health"><strong>${score}</strong><span>HEALTH SCORE</span><small>${esc(d.health?.status||'—')}</small></div><div class="preview-insights">${rows.length?rows.map(x=>`<div class="preview-insight ${esc(x.level)}"><i></i><strong>${esc(x.title)}</strong><span>${esc(x.metric||'')}</span></div>`).join(''):'<div class="insight-empty">Keine besonderen Auffälligkeiten.</div>'}</div></div>`;loaded=true;}catch(e){if(!/angemeldet/i.test(e.message))root.innerHTML=`<div class="insight-empty">${esc(e.message)}</div>`;}finally{loading=false;}}
  setInterval(load,800);
})();