{
  "name": "PalPanelServerMods",
  "version": "0.3.4",
  "description": "Server-only Community-Raid build with the proven Arena v2 3500-unit keep-in boundary unchanged. Step 2B disables the discovery scanner and adds exactly one vanilla BP_LevelObject_TowerLockBarrier_C visual probe at radius 3700. The selected Blueprint derives from PalLevelObject_LockedObstacle, whose actor and locked state are replicated by Palworld. No NPC spawn, custom RPC, Niagara call, collision mutation or AI mutation is used. No client installation required.",
  "clientInstallRequired": false,
  "capabilities": [
    "heartbeat",
    "ping",
    "event_pulse",
    "start_raid",
    "raid_status",
    "cancel_raid",
    "raid_damage_tracking",
    "raid_rewards",
    "raid_safe_runtime",
    "raid_wild_controller",
    "raid_native_combat",
    "arena_v2_center_diagnostics",
    "arena_v2_minimal_keep_in",
    "arena_v2_single_replicated_tower_lock_barrier_probe"
  ]
}